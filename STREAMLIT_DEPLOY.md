# Streamlit Community Cloud 部署指南

## ⚠️ 重要：仓库必须是 Public

**Streamlit Community Cloud 免费版要求：**
- ✅ **仓库必须是 Public（公开）**
- ❌ **Private（私有）仓库无法使用免费版**

**如果你的仓库是 Private：**
1. 将仓库改为 Public（推荐，免费）
2. 或使用 Streamlit 付费版（支持 Private 仓库）
3. 或使用其他部署方式（见下方"替代部署方案"）

### 如何将仓库改为 Public

1. 访问：https://github.com/jhuang7908/statics
2. 点击 "Settings"（设置）
3. 滚动到底部 "Danger Zone"（危险区域）
4. 点击 "Change visibility"（更改可见性）
5. 选择 "Make public"（设为公开）
6. 确认更改

## 快速部署步骤

### 1. 访问 Streamlit Community Cloud

打开浏览器，访问：
**https://share.streamlit.io/**

或直接访问：
**https://streamlit.io/cloud**

### 2. 登录

1. 点击 "Sign in" 或 "登录"
2. 选择 "Continue with GitHub"
3. 使用你的 GitHub 账号登录（`jhuang7908`）
4. 授权 Streamlit 访问你的 GitHub 仓库

### 3. 部署应用

1. 点击 "New app" 或 "新建应用"
2. 填写部署信息：
   - **Repository（仓库）**: 选择 `jhuang7908/statics`
   - **Branch（分支）**: 选择 `main`
   - **Main file path（主文件路径）**: 输入 `app.py`
   - **App URL（应用URL，可选）**: 可以自定义，如 `stat-ide` 或 `statics`
3. 点击 "Deploy" 或 "部署"

### 4. 等待部署完成

- Streamlit 会自动：
  - 安装依赖（从 `requirements.txt`）
  - 启动应用
  - 生成公开访问 URL

- 部署通常需要 1-3 分钟
- 可以在部署页面查看日志

### 5. 访问应用

部署成功后，你会获得一个 URL，例如：
- `https://stat-ide.streamlit.app`
- 或 `https://share.streamlit.io/jhuang7908/statics`

## 部署配置说明

### 已创建的配置文件

1. **`.streamlit/config.toml`** - Streamlit 配置文件
   - 主题设置
   - 服务器配置
   - 浏览器设置

2. **`requirements.txt`** - Python 依赖列表
   - 包含所有必需的包
   - Streamlit Cloud 会自动安装

### 重要注意事项

#### ⚠️ Ollama AI 功能限制

**当前状态：** AI 功能在云端无法使用

**原因：** 
- 应用依赖本地 Ollama 服务（`http://localhost:11434`）
- Streamlit Cloud 无法访问本地服务

**解决方案：**

1. **暂时禁用 AI 功能**（简单方案）
   - 其他统计功能可以正常使用
   - AI 聊天区域会显示连接错误

2. **配置远程 Ollama 服务**（推荐，如果需要 AI 功能）
   - 在云服务器上部署 Ollama
   - 在 Streamlit Cloud 的 Secrets 中配置 API 地址

#### 📝 配置远程 Ollama（可选）

如果需要启用 AI 功能：

1. 在 Streamlit Cloud 应用页面，点击 "Settings"（设置）
2. 选择 "Secrets"（密钥）
3. 添加以下内容：

```toml
[ollama]
api_url = "https://your-ollama-server.com:11434"
api_key = "your-api-key-if-needed"
```

4. 保存后，应用会自动重新部署

#### 🔒 数据隐私

- ⚠️ **应用是公开的**：所有上传的数据对所有访问者可见
- ⚠️ **请勿上传敏感数据**：如患者信息、个人隐私数据等
- 💡 **建议**：在应用首页添加数据隐私提示

## 更新应用

每次更新代码后：

1. 推送到 GitHub：
   ```bash
   git add .
   git commit -m "Update: 描述更改"
   git push origin main
   ```

2. Streamlit Cloud 会自动检测更改并重新部署（通常需要 1-2 分钟）

## 查看部署日志

如果部署失败：

1. 在 Streamlit Cloud 应用页面
2. 点击 "Manage app"（管理应用）
3. 查看 "Logs"（日志）标签
4. 检查错误信息

## 常见问题

### 问题 1: 部署失败 - 找不到依赖

**解决：** 检查 `requirements.txt` 是否包含所有依赖

### 问题 2: 应用无法启动

**解决：** 
- 查看部署日志
- 检查 `app.py` 是否有语法错误
- 确保所有导入的模块都在 `requirements.txt` 中

### 问题 3: 中文显示乱码

**解决：** 
- 确保文件使用 UTF-8 编码
- 检查 `.streamlit/config.toml` 配置

### 问题 4: AI 功能不工作

**解决：** 
- 这是预期的，因为 Ollama 在本地运行
- 需要配置远程 Ollama 服务或暂时禁用 AI 功能

## 替代部署方案（如果仓库是 Private）

如果你的仓库是 Private 且不想改为 Public，可以使用：

### 方案 1: Docker 部署（推荐）

在自己的服务器或云服务上部署：

```bash
# 创建 Dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

### 方案 2: 直接服务器部署

在云服务器（AWS、Azure、GCP 等）上：

```bash
# 安装依赖
pip install -r requirements.txt

# 运行应用
streamlit run app.py --server.port=8501 --server.address=0.0.0.0
```

### 方案 3: Streamlit 付费版

- 支持 Private 仓库
- 需要付费订阅

## 部署检查清单

部署前确认：

- [x] **仓库是 Public**（免费版要求）⚠️
- [x] 代码已推送到 GitHub (`jhuang7908/statics`)
- [x] `requirements.txt` 包含所有依赖
- [x] `.streamlit/config.toml` 已创建
- [x] `app.py` 是主文件
- [x] 分支是 `main`
- [ ] （可选）已配置远程 Ollama 服务（如果需要 AI 功能）

## 快速链接

- **Streamlit Community Cloud**: https://share.streamlit.io/
- **GitHub 仓库**: https://github.com/jhuang7908/statics
- **生成 Personal Access Token**: https://github.com/settings/tokens

